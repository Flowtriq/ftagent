# Optional dependencies — install for full functionality
# pip install ftagent[full]   installs all of these

scapy>=2.4.5       # Packet capture + PCAP support (required for traffic analysis)
psutil>=5.9.0      # System metrics collection
colorama>=0.4.6    # Colored terminal output (Windows compatibility)
